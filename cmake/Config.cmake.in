@PACKAGE_INIT@

find_package(ReactCommon CONFIG REQUIRED PATHS ${CMAKE_CURRENT_LIST_DIR} NO_DEFAULT_PATH)


if (@USE_JSC@)
	find_package(ReactCommonJSC CONFIG REQUIRED PATHS ${CMAKE_CURRENT_LIST_DIR} NO_DEFAULT_PATH)
endif()